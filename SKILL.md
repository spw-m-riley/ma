# ma skill

This file is a placeholder for the deterministic-first Copilot workflow.

The completed version will document:

1. when to run `ma`
2. which commands are deterministic and offline
3. where RTK fits
4. when an agent may optionally apply semantic polish after `ma` finishes
