# Example Project

read the docs before config begins.

## Setup

1. install the app to create the initial env.
2. keep repo config and auth notes in same directory.
3. but these command examples should stay unchanged.

```bash
ma compress README.md
```

For local validation, use `ma compress` with existing config and current env file.
