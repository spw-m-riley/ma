# Project Notes

The goal of this tool is to reduce tokens while still keeping the result understandable to both humans and language models.

Please remember to preserve paths like `/etc/hosts` and commands like `go test ./...`.

```bash
go test ./...
go build ./cmd/ma
```
